#!/bin/bash
#
# Launch Claude Code with translation ENABLED for this session only
# Usage: claude-x-on [claude args...]
#

export CLAUDE_SESSION_ID="session-$$"
echo "on" > "$HOME/.claude/translate-session-$CLAUDE_SESSION_ID"

# Cleanup on exit
cleanup() {
    rm -f "$HOME/.claude/translate-session-$CLAUDE_SESSION_ID"
}
trap cleanup EXIT

# Launch claude with session ID
exec claude "$@"
